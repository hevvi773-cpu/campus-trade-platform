# fix_patch_v9 运行依赖
Flask==3.1.1
PyMySQL==1.1.2
Werkzeug==3.1.3
# init_db 使用 sha256/caching_sha2 密码认证时需要，否则启动会报 cryptography 缺失警告（非致命，但建议装）
cryptography>=42.0.0
# 生产环境可选：gunicorn -w 4 -b 0.0.0.0:5000 app_v3:app
gunicorn>=21.0.0
